{
  "ts-node": {
    // Do not forget to `npm i -D tsconfig-paths`
    "require": ["tsconfig-paths/register"] // enables `ts-node` to resolve `@/*` alias paths
  },
  "compilerOptions": {},
  "exclude": ["node_modules"],
  "include": ["./apps/**/*.ts", "./cmd/**/*.ts"]
}
